containers:
  createpatient:
    port: 8080
    host: localhost
    basepath: /
    healthpath: actuator/health

  createprescription:
    port: 8080
    host: localhost
    basepath: /
    healthpath: actuator/health

  listcosts:
    port: 8081
    host: localhost
    basepath: /
    healthpath: actuator/health

  listpatients:
    port: 8081
    host: localhost
    basepath: /
    healthpath: actuator/health