kind: PersistentVolume
apiVersion: v1
metadata:
  name: patient-db-pv-volume
  labels:
    type: local
    app: patient-db
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"